---
milestone: M003
slice: S01
task: T03
project: SunoFlow
created: 2026-05-28T10:10:00Z
status: planned
---

# M003-S01-T03 -- Task Plan

**Summary:** End-to-end Verifikation des `/api/mcp` Endpoints. Original-Plan: MCP Inspector gegen lokalen Dev-Server. Geliefert: wiederverwendbares Smoke-Skript `scripts/smoke-mcp.mjs` das `initialize` → `tools/list` → `tools/call sunoflow_info` gegen jede URL fährt + Doc-Update in `docs/MCP.md`.

## Files

- **Create:** `scripts/smoke-mcp.mjs` -- Node CLI, fetch-basiert, parsed JSON + SSE
- **Modify:** `docs/MCP.md` -- restrukturiert, beide Transports dokumentiert, Smoke-Test Section

## What this does

`scripts/smoke-mcp.mjs`:
- Default-URL `http://localhost:3000/api/mcp`, override per CLI-Arg
- Liest `SUNOFLOW_API_KEY` aus env (Exit 2 wenn nicht gesetzt)
- 3-Step JSON-RPC-Roundtrip mit Assertions (status, serverInfo.name, tools-Array enthält `sunoflow_info`, tool-call returnt server version)
- Content-Type-Sniffing: parsed `application/json` direkt, parsed `text/event-stream` per `data:`-Line-Split (Streamable-HTTP MAY antworten beide Modi)
- Exit 0 success, 1 check-fail, 2 missing env

## Verification

```bash
node --check scripts/smoke-mcp.mjs
# Live-Run gegen Dev-Server: requires running postgres + pnpm dev
SUNOFLOW_API_KEY=sk-... node scripts/smoke-mcp.mjs http://localhost:3000/api/mcp
```

Caveat: lokaler Live-Run nicht in dieser Session möglich (Docker daemon down). Live-Verifikation auf deployed sunoflow.app wird in S04-T05 ausgeführt.

## Done signal

Skript Syntax-Check pass + Docs aktualisiert. Voll-E2E in S04-T05.
