---
milestone: M001
slice: S02
task: T04
project: SunoFlow
created: 2026-05-18T09:15:00Z
status: planned
---

# M001-S02-T04 -- Task Plan

**Summary:** `.ytstack/M001-IA-MAP.md` schreiben: Vorher/Nachher-Diagramm von 25 Top-Level-Routen auf reduzierte Top-Level-Nav (Ziel ~10-12 Items). Pro Konsolidierung: alte Route(n) → neue Heimat + Begruendung (frequency, semantic-fit, journey-station). Decision-Entries fuer DECISIONS.md. Plan-only.

## Files

**Create:**
- `.ytstack/M001-IA-MAP.md` -- IA-Konsolidierungs-Map mit Mapping-Tabelle + Vorher/Nachher-Diagramm (ASCII) + Begruendungen.

**Modify:**
- `.ytstack/DECISIONS.md` -- 12 Multi-Home-Decisions + 1 Source-of-Truth Decision formal eintragen.

**Read-only (Quellen):**
- `.ytstack/USER-JOURNEY.md` §1 (Persona + Primary Loop), §9.5 (12 Multi-Home-Decisions mit Default-Vorschlaegen)
- `.ytstack/M001-ROUTE-CATALOG.md` (heutige 56 Pages)
- `.ytstack/M001-COMPONENT-MAP.md` (god-object Konsequenzen pro Konsolidierung)
- `.ytstack/M001-MOBILE-AUDIT.md` (Bottom-Nav-Decision auf Mobile)

**Delete:** none.

## What this does

**Pass 1 -- IA-MAP.md Skeleton:**

```
§1 Vorher: 17 Nav-Items + 25 Top-Level Routen (ASCII)
§2 Nachher: ~10-12 Items mit Sub-Tabs/Sub-Routes (ASCII)
§3 Mapping-Tabelle: alte Route(n) → neue Heimat (eine Zeile pro Konsolidierung)
§4 Begruendung pro Konsolidierung (frequency / semantic-fit / journey-station)
§5 Mobile-Nav-Decision (Bottom-Nav vs einheitlicher Drawer)
§6 Out-of-Scope (Admin, Public, Cross-Cuts wie /pricing /settings)
```

**Pass 2 -- DECISIONS.md Entries** (13 entries):

```
- D1: FEATURE-MAP §2 als Source-of-Truth fuer Heimat-Zuweisungen (Begruendung: Inventory 19% gedriftet)
- D2: /inspire → §3 Generate Primary
- D3: /compare → §3 Generate sub-Tab
- D4: /discover + /explore merge zu /discover mit Tabs
- D5: /radio → §7 Discover Primary, Secondary in §4 Listen
- D6: /songs → kill, merge in /library
- D7: /discover/collections/[id] → /library/collections/[id] (URL-Fix)
- D8: /users/[id] → /profile/[id], /u/[username] bleibt public-only
- D9: Analytics-5-Cluster → /analytics mit Sub-Tabs + /admin/analytics
- D10: Generate-Cluster → /generate-Tabs + /generations + /inspire separat
- D11: Authoring-3-Cluster → /authoring Hub
- D12: Persona-Auswahl bleibt inline in GenerateForm
- D13: /library/[id] Primary=§6 Refine
```

**Pass 3 -- Migration-Strategie-Skizze** (kurz, full details kommt in S03-T04):
- Feature-Flags / parallel-Routes / redirect-table als drei Optionen genannt, Empfehlung in S03.

## Verification

```bash
test -f .ytstack/M001-IA-MAP.md && \
  echo "IA_SECTIONS=$(grep -cE '^## ' .ytstack/M001-IA-MAP.md)" && \
  echo "MAPPING_ROWS=$(grep -cE '^\| `/' .ytstack/M001-IA-MAP.md)" && \
  echo "DECISIONS_ADDED=$(grep -cE '^### D[0-9]+' .ytstack/DECISIONS.md)" && \
  echo "ASCII_DIAGRAMS=$(grep -cE '^```$' .ytstack/M001-IA-MAP.md)"
```

Success: `IA_SECTIONS >= 5`, `MAPPING_ROWS >= 20` (eine Zeile pro alter-Route-Konsolidierung), `DECISIONS_ADDED >= 13`, `ASCII_DIAGRAMS >= 2` (Vorher + Nachher).

## Done signal

`/ytstack:summarize-task`. Beruehrt Meta: `.ytstack/M001-S02-T04-SUMMARY.md`, `.ytstack/M001-S02-PLAN.md`, `.ytstack/STATE.md`. (Hook-Bug Issue #19.)
