---
milestone: M005
slice: S01
task: T05
project: SunoFlow
created: 2026-07-22T13:50:00Z
status: planned
---

# M005-S01-T05 -- Task Plan

**Summary:** Completion hook: on generation completion the song joins the session playlist and is flagged for host-queue append; unit tests on the seam.

## Files

- Create: `src/lib/jam/completion.ts` (`syncJamEntryOnCompletion(songId, "ready" | "failed")`)
- Create: `src/lib/jam/completion.test.ts`
- Modify: `src/lib/jam/index.ts` (export)
- Modify: `src/lib/generation/song-completion.ts` (wire: tracked side-effect in `handleSongSuccess`, best-effort call in `handleSongFailure`)
- Modify: `src/lib/generation/song-completion.test.ts` (mock `@/lib/jam`)

## What this does

The song-completion seam (`handleSongSuccess` side-effect list /
`handleSongFailure`) is the single place terminal generation states are
processed — the jam hook attaches there, NOT via direct prisma updates
elsewhere (lifecycle-seam rule). `syncJamEntryOnCompletion`: no-op for
non-jam songs (unique songId lookup); vetoed entries stay vetoed and never
join the playlist; ready → append song to the session playlist
(P2002-tolerant, position = count) + entry pending→ready (this status flip
IS the host-queue-append flag S02 polls for); failed → entry pending→failed
(frees the guest's open-prompt slot).

## Verification

```bash
npx vitest run src/lib/jam/ src/lib/generation/
npx tsc --noEmit && pnpm build
```

Success: command exits 0 and shows expected output. Failure: re-read plan, check if spec drifted.

## Done signal

Run `/ytstack:summarize-task` after execution to mark complete and advance STATE.md.
