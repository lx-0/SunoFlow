---
milestone: M005
slice: S03
task: T03
project: SunoFlow
created: 2026-07-22T18:30:00Z
status: planned
---

# M005-S03-T03 -- Task Plan

**Summary:** Guest nickname: one-time capture (localStorage), attached to prompts and shown on cards.

## Files

- Modify: `src/components/JamGuestView.tsx` (name input above the chips, localStorage persistence, guestName on push)

## What this does

A small "Your name" input above the vibe chips, persisted to
`sunoflow-jam-guest-name` on change (private-mode tolerant); trimmed and
sent as `guestName` with every prompt push. Cards already render
`guestName ?? "Guest"` (T01), so no list changes needed.

## Verification

```bash
npx tsc --noEmit && pnpm build
# keyless runtime smoke: name + prompt → card shows the name; reload keeps the name
```

Success: command exits 0 and shows expected output. Failure: re-read plan, check if spec drifted.

## Done signal

Run `/ytstack:summarize-task` after execution to mark complete and advance STATE.md.
