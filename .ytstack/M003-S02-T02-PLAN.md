---
milestone: M003
slice: S02
task: T02
project: SunoFlow
created: 2026-05-28T10:25:00Z
status: planned
---

# M003-S02-T02 -- Task Plan

**Summary:** Side-effect imports aller 15 Tool-Module + Wiring der Tools-Handler (`ListTools`, `CallTool`) in den HTTP-Route-Pfad.

## Files

- **Create:** `src/lib/mcp/registry-bootstrap.ts` -- single import barrel (15 tools + 4 providers)
- **Create:** `src/lib/mcp/register-handlers.ts` -- `registerMcpHandlers(server, userId)` shared between stdio + http
- **Modify:** `src/app/api/mcp/route.ts` -- replace stub registration with shared wiring

## What this does

- `registry-bootstrap.ts` performs 19 side-effect imports (15 tools + 4 providers) at module load. Idempotent because registries use Map/Set semantics.
- `register-handlers.ts` exports a single `registerMcpHandlers(server, userId)` that does all 5 `setRequestHandler` calls (tools + resources, see T03/T04).
- Route handler imports `registry-bootstrap` once and calls `registerMcpHandlers(server, userId)` inside `buildServerFor`.

## Verification

```bash
pnpm vitest run src/app/api/mcp/route.test.ts mcp/server.test.ts
pnpm tsc --noEmit
```

Route test must list all 16 registered tools (sunoflow_info + 15 production tools + playlist registers create+add = 16 total names).
