---
milestone: M001
slice: S02
task: T03
project: SunoFlow
created: 2026-05-18T09:00:00Z
status: planned
---

# M001-S02-T03 -- Task Plan

**Summary:** Coverage-Matrix in `.ytstack/USER-JOURNEY.md` §9 fuellen. Jedes Feature aus S01-Outputs (56 Pages + ~224 API routes + 101 Components + 4 dead) bekommt genau eine Primary Home (§2-§8) plus optional Secondary Entry Points. Orphans + Multi-Home Features explizit markieren. Hartes Kriterium: kein Feature outside the journey. Plan-only.

## Files

**Modify:**
- `.ytstack/USER-JOURNEY.md` -- ersetze §9 Placeholder mit Coverage-Matrix.

**Read-only (Quellen):**
- `.ytstack/USER-JOURNEY.md` §1-§8 (Persona + 7 Hauptpfade als Klassifikations-Buckets)
- `.ytstack/M001-ROUTE-CATALOG.md` (56 Pages + 224 API)
- `.ytstack/M001-COMPONENT-MAP.md` (101 Top-Components + 4 dead + 6 Subdir-Cluster)
- `.ytstack/M001-FEATURE-GAPS.md` (12 Cross-Map-Mismatches needing decision, 50+ undocumented features)
- `.ytstack/FEATURE-MAP.md` §2 (10 Bounded Contexts)

**Delete:** none.

## What this does

Pro Klassifikations-Bucket (§2-§8) eine Tabelle mit drei Spalten:
| Item | Heimat | Notes |
|---|---|---|

§9 strukturiert als:

**§9.1 Pages Coverage (56 Pages + 5 Public Surfaces).** Pro Page: Primary Home Station (§2-§8) + Secondary Entry Points + Status (live / dead / orphan / multi-home decision).

**§9.2 API Routes Coverage (224 routes).** Gruppiert nach Mount-Prefix (siehe ROUTE-CATALOG §C). Pro Gruppe: Primary Home Station.

**§9.3 Components Coverage (101 + 6 subdirs).** Pro god-object + jeder Top-10-by-churn + dead-code candidates: Heimat.

**§9.4 Orphans.** Features die in keine §-Station passen. Format: Feature + Begruendung + Vorschlag (delete / re-home / new-section).

**§9.5 Multi-Home Features.** Features mit echtem Anspruch auf 2+ Heimaten. 12 Cross-Map-Mismatches aus FEATURE-GAPS §D plus Findings aus T02 (z.B. `/inspire` = Generate+Discover). Pro Item: Conflict + Decision-Vorschlag.

**§9.6 Coverage Audit.** Zaehl-Summe -- alle 56 Pages, 224 API routes, 101 Components zugewiesen. Etwaige Luecken explizit benennen.

**Klassifikations-Regel:** FEATURE-MAP §2 ist Source-of-Truth (per T01 Decision-Sketch). Inventory-Mismatches aus FEATURE-GAPS §D werden nach FEATURE-MAP entschieden.

## Verification

```bash
test -f .ytstack/USER-JOURNEY.md && \
  echo "COVERAGE_SUBSECTIONS=$(grep -cE '^### §9\.' .ytstack/USER-JOURNEY.md)" && \
  echo "PAGES_COUNTED_HEURISTIC=$(awk '/^### §9\.1/,/^### §9\.2/' .ytstack/USER-JOURNEY.md | grep -cE '^\| `/')" && \
  echo "ORPHANS_FLAGGED=$(awk '/^### §9\.4/,/^### §9\.5/' .ytstack/USER-JOURNEY.md | grep -cE '^- ')" && \
  echo "MULTI_HOME_DECISIONS=$(awk '/^### §9\.5/,/^### §9\.6/' .ytstack/USER-JOURNEY.md | grep -cE '^- ')" && \
  echo "STILL_PLACEHOLDER=$(grep -cE '\(to be filled' .ytstack/USER-JOURNEY.md)"
```

Success: `COVERAGE_SUBSECTIONS=6` (§9.1-§9.6). `PAGES_COUNTED_HEURISTIC >= 30` (sample of pages listed in §9.1). `ORPHANS_FLAGGED >= 4` (mindestens die 4 dead-code candidates). `MULTI_HOME_DECISIONS >= 5`. `STILL_PLACEHOLDER=0` (alle Placeholder weg).

## Done signal

`/ytstack:summarize-task`. Beruehrt Meta: `.ytstack/M001-S02-T03-SUMMARY.md`, `.ytstack/M001-S02-PLAN.md`, `.ytstack/STATE.md`. (Hook-Bug Issue #19.)
