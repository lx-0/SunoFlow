---
milestone: M001
slice: S02
task: T02
project: SunoFlow
created: 2026-05-18T08:45:00Z
status: planned
---

# M001-S02-T02 -- Task Plan

**Summary:** Sieben Journey-Hauptpfade als §2-§8 in `.ytstack/USER-JOURNEY.md` fuellen. Pro Pfad: Steps (wo bin ich, was klicke ich, was sehe ich), Surfaces (aus ROUTE-CATALOG), Components (aus COMPONENT-MAP), Friction-Points (aus FRICTION-AUDIT). Plan-only.

## Files

**Modify:**
- `.ytstack/USER-JOURNEY.md` -- ersetze die 7 Placeholder-Sektionen §2-§8 mit echtem Inhalt. §9 bleibt Placeholder fuer T03.

**Read-only (Quellen):**
- `.ytstack/USER-JOURNEY.md` §1 (Persona + Loop als Constraint)
- `.ytstack/M001-ROUTE-CATALOG.md` (56 Pages, Bounded-Context-Gruppen)
- `.ytstack/M001-COMPONENT-MAP.md` (101 Components + Subdirs)
- `.ytstack/M001-FEATURE-GAPS.md` (50+ undocumented Features fuer Vollstaendigkeits-Check)
- `.ytstack/M001-FRICTION-AUDIT.md` (6 god-objects in Zahlen)
- `.ytstack/M001-MOBILE-AUDIT.md` (mobile-specific Patterns)

**Delete:** none.

## What this does

Pro Pfad (§2-§8) ein konsistentes Template:

```
## §N. <Loop Name>

**Primary loop step:** <1-4 oder secondary>
**Surface(s):** <list of routes from ROUTE-CATALOG>
**Components:** <key god-objects + supporting components>

### Steps
1. **<Step name>** -- where am I, what do I click, what do I see
2. ...

### Friction points
- <quantified from FRICTION-AUDIT or ROUTE-CATALOG>
- ...

### Mobile-specific notes
- <from MOBILE-AUDIT where relevant>

### Open IA questions for T04
- <what S02-T04 needs to decide>
```

**Die 7 Pfade in Reihenfolge:**

| § | Pfad | Surfaces | god-object |
|---|---|---|---|
| §2 | Onboarding | `/`, `/register`, `/login`, `/verify-email`, ApiKeyWizard, OnboardingTour, Confetti | `LandingPage` |
| §3 | Generate | `/generate`, `/mashup`, `/inspire`, `/compare`, `/generations` | `GenerateForm` |
| §4 | Listen | GlobalPlayer (jede Page), `/library`, `/library/[id]`, `/songs`, `/history` | `GlobalPlayer` + `QueueContext` |
| §5 | Organize | `/library`, `/favorites`, `/playlists`, `/playlists/[id]`, `/history` | `LibraryView` |
| §6 | Refine | `/library/[id]` (SongDetailView) + 7 modals | `SongDetailView` |
| §7 | Discover | `/discover`, `/explore`, `/radio`, `/feed`, `/inspire`, `/users/[id]` | `DiscoverView` (geteilt) |
| §8 | Share & Engage | `/s/[slug]`, `/p/[slug]`, `/u/[username]`, `/embed/*`, `/notifications`, `/stats`, `/insights`, `/analytics`, `/dashboard/analytics` | `PublicSongView`, `NotificationsView` |

Steps muessen pragmatisch sein, nicht aspirational: was passiert HEUTE im Code, nicht was passieren sollte.

Friction-Refs: explizit auf die FRICTION-AUDIT-Sektion zeigen ("siehe §1: 30 useState in GenerateForm"), nicht Zahlen duplizieren.

Mobile-Notes nur wo MOBILE-AUDIT was relevantes hat (z.B. §4 Listen: ExpandedPlayer-Trigger, §5 Organize: SwipeablePlaylistItem, §6 Refine: 7 Modals werden zu was auf Mobile?).

## Verification

```bash
test -f .ytstack/USER-JOURNEY.md && \
  echo "FILLED_SECTIONS=$(grep -cE '^### Steps$' .ytstack/USER-JOURNEY.md)" && \
  echo "FRICTION_REFS=$(grep -cE '^### Friction points$' .ytstack/USER-JOURNEY.md)" && \
  echo "STILL_PLACEHOLDER=$(grep -cE '\(to be filled' .ytstack/USER-JOURNEY.md)"
```

Success: `FILLED_SECTIONS=7` (one ### Steps per §2-§8), `FRICTION_REFS=7`, `STILL_PLACEHOLDER=1` (only §9 Coverage Matrix bleibt fuer T03).

## Done signal

`/ytstack:summarize-task` nach Execution. Touched Meta: `.ytstack/M001-S02-T02-SUMMARY.md`, `.ytstack/M001-S02-PLAN.md`, `.ytstack/STATE.md`. (Hook-Bug Issue #19.)
